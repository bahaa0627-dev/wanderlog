# 地点名称超链接功能

## 功能概述

AI助手回复中，所有匹配到数据库的地点名称都会自动转换为可点击的超链接，点击后直接打开地点详情页。

## 实现细节

### 地点名称匹配

1. **优先级匹配**：按地点名长度降序匹配，避免短名称误匹配
   - 例如："海德公园"会优先匹配，而不是"公园"

2. **忽略大小写**：支持大小写不敏感匹配
   - "Hyde Park" 和 "hyde park" 都能匹配到 "Hyde Park"

3. **避免重复匹配**：
   - 如果文本已被标记为加粗或链接，不会重复匹配
   - 每个地点名的所有出现都会被标记为可点击

### 显示样式

#### 加粗地点名（**地点名**）
- 颜色：蓝色 (`AppTheme.accentBlue`)
- 字重：700 (bold)
- 字号：16px
- 装饰：下划线
- 评分：显示在地点名后（如果有）

#### 普通文本地点名
- 颜色：蓝色 (`AppTheme.accentBlue`)
- 字重：600 (semi-bold)
- 字号：继承当前字号
- 装饰：下划线
- 评分：显示在地点名后（如果有）

### 点击行为

点击地点名称后：
1. 触发 `_showPlaceDetail(place)` 方法
2. 如果地点有数据库UUID且缺少详情，会先从后端获取完整数据
3. 打开地点详情模态框 (`UnifiedSpotDetailModal`)
4. 显示地点的完整信息（图片、评分、标签、开放时间等）

### 调试信息

开启调试模式后，控制台会输出：
```
🔍 Searching for place names in text. Total places: 5
✅ Linked "海德公园" (2 occurrences)
✅ Linked "大英博物馆" (1 occurrence)
🔗 Total place links created: 3
```

## 用户体验

### 示例效果

**输入文本**：
```
第一天上午可以去海德公园散步，下午参观大英博物馆。海德公园环境优美。
```

**显示效果**：
- [海德公园](#)（带下划线，蓝色）散步
- [大英博物馆](#)（带下划线，蓝色）
- [海德公园](#)（带下划线，蓝色）环境优美

所有蓝色下划线文本都可点击跳转到详情页。

## 技术栈

- **Flutter**: 富文本渲染 (`RichText`, `TextSpan`)
- **手势识别**: `TapGestureRecognizer` 处理点击事件
- **正则表达式**: 地点名称匹配（支持转义和忽略大小写）
- **模态框**: `UnifiedSpotDetailModal` 显示地点详情

## 相关文件

- `wanderlog_app/lib/features/ai_recognition/presentation/pages/ai_assistant_page.dart`
  - `_buildRichText()`: 富文本渲染和地点匹配
  - `_showPlaceDetail()`: 打开地点详情页
  - `_RichTextMatch`: 匹配项数据模型

## 后续优化建议

1. **模糊匹配**：支持地点别名匹配（如"卢浮"匹配"卢浮宫"）
2. **高亮效果**：首次出现的地点可以加粗+图标突出显示
3. **缓存优化**：已加载的地点详情可缓存，减少重复请求
4. **智能提示**：长按地点名显示快捷预览卡片
