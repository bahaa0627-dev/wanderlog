# 🌍 公共地点库 - 完整系统指南

## 📖 目录

1. [系统概述](#系统概述)
2. [核心文件](#核心文件)
3. [快速开始](#快速开始)
4. [API 使用](#api-使用)
5. [数据管理](#数据管理)
6. [常见问题](#常见问题)

---

## 系统概述

公共地点库是一个完整的地点信息管理系统，支持从多个来源导入和管理全球地点数据。

### 主要特性

✅ **唯一性保证** - 基于 Google Place ID 去重
✅ **多源导入** - 支持链接、图片、对话三种方式
✅ **AI 增强** - 自动生成标签和描述
✅ **可视化管理** - Prisma Studio 实时编辑
✅ **RESTful API** - 完整的 CRUD 操作
✅ **高度扩展** - 支持自定义字段

### 系统架构

```
┌─────────────────────────────────────────────────────┐
│              数据来源 (3种方式)                        │
├──────────────┬──────────────┬─────────────────────┤
│ Google Maps  │   用户图片    │   AI 对话推荐         │
│   收藏链接    │              │                     │
└──────┬───────┴──────┬───────┴──────────┬──────────┘
       │              │                  │
       ▼              ▼                  ▼
┌─────────────────────────────────────────────────────┐
│                 处理层                                │
│  Apify Scraper │ AI Vision  │ AI Chat Agent        │
└──────┬───────────────┬──────────────┬───────────────┘
       │               │              │
       └───────────────┴──────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│              Google Maps API                         │
│         (获取 place_id 和详细信息)                    │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│             PublicPlace 数据库                        │
│              (SQLite + Prisma)                       │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│                RESTful API                           │
│            (Express + TypeScript)                    │
└──────────────────────┬──────────────────────────────┘
                       │
           ┌───────────┴───────────┐
           ▼                       ▼
    ┌─────────────┐         ┌─────────────┐
    │ 前端应用     │         │ Prisma      │
    │ (Flutter)   │         │ Studio      │
    └─────────────┘         └─────────────┘
```

---

## 核心文件

### 📂 项目结构

```
wanderlog/
├── wanderlog_api/                    # 后端 API
│   ├── prisma/
│   │   └── schema.prisma             # 数据库模型（包含 PublicPlace）
│   ├── src/
│   │   ├── controllers/
│   │   │   └── publicPlaceController.ts    # 控制器
│   │   ├── routes/
│   │   │   └── publicPlaceRoutes.ts        # 路由
│   │   ├── services/
│   │   │   ├── publicPlaceService.ts       # 核心服务
│   │   │   ├── googleMapsService.ts        # Google Maps
│   │   │   ├── apifyService.ts             # Apify 集成
│   │   │   └── aiService.ts                # AI 集成
│   │   └── index.ts                        # 主入口
│   └── .env                                # 环境变量配置
│
├── PUBLIC_PLACES_LIBRARY_README.md          # 完整技术文档
├── PUBLIC_PLACES_QUICK_START.md             # 快速开始指南
├── PUBLIC_PLACES_IMPLEMENTATION_SUMMARY.md  # 实施总结
├── PUBLIC_PLACES_API.postman_collection.json # Postman 测试
└── test_public_places_api.sh                # 测试脚本
```

### 📄 文档说明

| 文件 | 用途 | 何时使用 |
|------|------|----------|
| `PUBLIC_PLACES_QUICK_START.md` | 快速开始 | 第一次使用 |
| `PUBLIC_PLACES_LIBRARY_README.md` | 完整文档 | 深入了解 |
| `PUBLIC_PLACES_IMPLEMENTATION_SUMMARY.md` | 实施总结 | 查看完成状态 |
| `PUBLIC_PLACES_API.postman_collection.json` | API 测试 | 测试 API |
| `test_public_places_api.sh` | 自动测试 | 快速验证 |

---

## 快速开始

### 步骤 1：启动服务

```bash
# 在第一个终端 - 启动 API 服务器
cd wanderlog_api
npm run dev

# 在第二个终端 - 启动 Prisma Studio（可选）
cd wanderlog_api
npm run db:studio
```

### 步骤 2：测试系统

```bash
# 运行自动测试脚本
./test_public_places_api.sh

# 或手动测试
curl http://localhost:3000/api/public-places/stats
```

### 步骤 3：添加第一个地点

```bash
# 添加埃菲尔铁塔
curl -X POST http://localhost:3000/api/public-places/add-by-place-id \
  -H "Content-Type: application/json" \
  -d '{"placeId": "ChIJLU7jZClu5kcR4PcOOO6p3I0"}'
```

### 步骤 4：查看数据

- **方式 A**：浏览器访问 http://localhost:5555 （Prisma Studio）
- **方式 B**：API 查询
  ```bash
  curl http://localhost:3000/api/public-places
  ```

---

## API 使用

### 基础查询

```bash
# 获取所有地点
GET /api/public-places?page=1&limit=20

# 搜索地点
GET /api/public-places/search?q=coffee

# 获取详情
GET /api/public-places/:placeId

# 统计信息
GET /api/public-places/stats

# 按条件筛选
GET /api/public-places?city=Paris&category=咖啡馆
```

### 三种导入方式

#### 方式 1：手动添加（推荐开始）

```bash
POST /api/public-places/add-by-place-id
Body: { "placeId": "ChIJ..." }
```

**无需额外配置，立即可用！**

#### 方式 2：从 Google Maps 链接导入

```bash
POST /api/public-places/import-from-link
Body: { "url": "https://www.google.com/maps/saved/..." }
```

**需要配置**：`APIFY_API_TOKEN`

#### 方式 3：从图片识别导入

```bash
POST /api/public-places/import-from-image
Body: { "imageUrl": "https://example.com/image.jpg" }
```

**需要配置**：`OPENAI_API_KEY` 或 `GEMINI_API_KEY`

#### 方式 4：从对话导入

```bash
POST /api/public-places/import-from-chat
Body: { 
  "message": "推荐巴黎的浪漫咖啡馆",
  "city": "Paris"
}
```

**需要配置**：`OPENAI_API_KEY`

### 管理操作

```bash
# 更新地点
PUT /api/public-places/:placeId
Body: { "name": "新名称", "category": "新分类" }

# 删除地点
DELETE /api/public-places/:placeId

# 同步 Google 数据
POST /api/public-places/:placeId/sync

# 生成 AI 标签
POST /api/public-places/:placeId/generate-tags
```

---

## 数据管理

### 使用 Prisma Studio

Prisma Studio 是最简单的数据管理方式：

```bash
npm run db:studio
```

访问 http://localhost:5555

**功能**：
- ✅ 查看所有数据
- ✅ 搜索和筛选
- ✅ 添加新记录
- ✅ 编辑现有记录
- ✅ 删除记录
- ✅ 查看关系

### 数据字段说明

**必填字段**：
- `placeId` - Google Place ID（唯一）
- `name` - 地点名称
- `latitude` - 纬度
- `longitude` - 经度
- `address` - 地址
- `city` - 城市
- `country` - 国家
- `category` - 分类
- `coverImage` - 封面图
- `aiTags` - AI 标签（JSON）
- `aiSummary` - AI 简介

**可选字段**：
- `images` - 图片数组（JSON）
- `rating` - 评分
- `website` - 网站

**扩展字段**：
- `customFields` - 自定义数据（JSON）

### 数据导入策略

#### 推荐顺序：

1. **手动添加核心地点**
   - 使用 Google Maps 找到重要地点的 Place ID
   - 通过 API 逐个添加
   - 最准确，无需额外配置

2. **批量导入（可选）**
   - 配置 Apify
   - 从 Google Maps 收藏列表批量导入

3. **AI 增强（可选）**
   - 配置 OpenAI
   - 为现有地点生成标签和描述

---

## 常见问题

### Q1: 如何获取 Google Place ID？

**方法 1**：通过 Google Maps
1. 访问 https://www.google.com/maps
2. 搜索地点
3. 右键 → "关于此地点" → 复制 Place ID

**方法 2**：使用现成的 Place ID
- 埃菲尔铁塔: `ChIJLU7jZClu5kcR4PcOOO6p3I0`
- 卢浮宫: `ChIJD7fiBh9u5kcRYJSMaMOCCwQ`
- 凯旋门: `ChIJjx37cOxv5kcRP2UrGDD8x_I`

### Q2: 需要配置哪些 API Keys？

**立即可用**（已配置）：
- ✅ Google Maps API

**可选功能**（需要配置）：
- ⬜ Apify API Token - 用于链接导入
- ⬜ OpenAI API Key - 用于 AI 功能
- ⬜ Gemini API Key - OpenAI 的备选

### Q3: 如何确保地点不重复？

系统自动基于 `placeId` 去重：
- 每个 Google Place ID 全球唯一
- 数据库设置了唯一约束
- 使用 `upsert` 操作（存在则更新）

### Q4: 可以手动编辑地点信息吗？

可以！两种方式：
1. **Prisma Studio** - 可视化界面编辑
2. **API** - 通过 PUT 请求更新

除了 `placeId`，所有字段都可以修改。

### Q5: 如何定期更新 Google Maps 数据？

```bash
# 同步单个地点
curl -X POST http://localhost:3000/api/public-places/:placeId/sync

# 可以设置定时任务批量同步
```

### Q6: 数据存储在哪里？

SQLite 数据库文件：
```
wanderlog_api/prisma/dev.db
```

可以直接备份此文件。

### Q7: 如何导出数据？

**方法 1**：通过 API
```bash
curl http://localhost:3000/api/public-places?limit=1000 > places.json
```

**方法 2**：直接复制数据库
```bash
cp wanderlog_api/prisma/dev.db backup.db
```

### Q8: 系统支持多少个地点？

SQLite 理论上可以存储数百万条记录，但建议：
- 开发环境：< 10,000 地点
- 生产环境：考虑迁移到 PostgreSQL

### Q9: 如何测试 AI 功能？

需要先配置 API Key：

```bash
# 在 .env 中添加
OPENAI_API_KEY=sk-xxxxx

# 重启服务器
npm run dev

# 测试图片识别
curl -X POST http://localhost:3000/api/public-places/import-from-image \
  -H "Content-Type: application/json" \
  -d '{"imageUrl": "https://example.com/photo.jpg"}'
```

### Q10: 可以在生产环境使用吗？

当前实现适合：
- ✅ 开发和测试
- ✅ 小到中型项目
- ✅ MVP 原型

生产环境建议：
- 迁移到 PostgreSQL
- 添加认证和授权
- 实现速率限制
- 添加缓存层
- 设置监控和日志

---

## 🎯 推荐学习路径

### 第 1 天：熟悉基础
1. ✅ 启动服务器
2. ✅ 打开 Prisma Studio
3. ✅ 手动添加 3-5 个地点
4. ✅ 测试查询和搜索

### 第 2 天：深入 API
1. ✅ 使用 Postman 测试所有端点
2. ✅ 尝试更新和删除操作
3. ✅ 测试筛选和分页

### 第 3 天：高级功能（可选）
1. ⬜ 配置 Apify 测试链接导入
2. ⬜ 配置 OpenAI 测试 AI 功能
3. ⬜ 批量导入测试数据

### 第 4 天：集成应用
1. ⬜ 在 Flutter 应用中调用 API
2. ⬜ 实现地点列表展示
3. ⬜ 添加搜索功能

---

## 📚 相关资源

### 文档
- [完整技术文档](PUBLIC_PLACES_LIBRARY_README.md)
- [快速开始指南](PUBLIC_PLACES_QUICK_START.md)
- [实施总结](PUBLIC_PLACES_IMPLEMENTATION_SUMMARY.md)

### API 工具
- Postman 集合：`PUBLIC_PLACES_API.postman_collection.json`
- 测试脚本：`test_public_places_api.sh`

### 外部服务文档
- [Google Maps Places API](https://developers.google.com/maps/documentation/places/web-service)
- [Apify Platform](https://docs.apify.com/)
- [OpenAI API](https://platform.openai.com/docs)
- [Prisma Documentation](https://www.prisma.io/docs)

---

## 🚀 立即开始

```bash
# 1. 启动服务
cd wanderlog_api && npm run dev

# 2. 打开新终端，启动 Prisma Studio
cd wanderlog_api && npm run db:studio

# 3. 测试 API
curl http://localhost:3000/api/public-places/stats

# 4. 添加第一个地点
curl -X POST http://localhost:3000/api/public-places/add-by-place-id \
  -H "Content-Type: application/json" \
  -d '{"placeId": "ChIJLU7jZClu5kcR4PcOOO6p3I0"}'
```

**恭喜！你已经完全掌握了公共地点库系统！** 🎉
