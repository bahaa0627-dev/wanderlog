# Wanderlog 项目架构说明

## 📁 项目结构

```
wanderlog/
├── wanderlog_app/              # Flutter 跨平台应用
│   ├── lib/
│   │   ├── core/              # 核心功能模块
│   │   │   ├── constants/     # 应用常量
│   │   │   ├── network/       # 网络层（Dio 客户端）
│   │   │   ├── storage/       # 存储层（Secure Storage, SharedPreferences）
│   │   │   └── utils/         # 工具类（路由、工具函数）
│   │   ├── features/          # 功能模块
│   │   │   ├── auth/          # 认证模块
│   │   │   ├── trips/         # 行程模块
│   │   │   ├── maps/          # 地图模块
│   │   │   ├── spots/         # 地点模块
│   │   │   └── payment/       # 支付模块
│   │   ├── shared/            # 共享组件
│   │   │   ├── widgets/       # 通用 Widget
│   │   │   └── models/        # 共享数据模型
│   │   ├── services/          # 服务层
│   │   └── main.dart          # 应用入口
│   ├── assets/                # 资源文件
│   │   ├── images/            # 图片资源
│   │   └── icons/              # 图标资源
│   ├── test/                   # 测试文件
│   ├── android/                # Android 平台配置
│   ├── ios/                    # iOS 平台配置
│   ├── web/                    # Web 平台配置
│   ├── pubspec.yaml            # Flutter 依赖配置
│   └── analysis_options.yaml   # Dart 代码规范配置
│
├── wanderlog_api/              # 后端 API 服务
│   ├── src/
│   │   ├── controllers/       # 路由控制器
│   │   ├── services/          # 业务逻辑服务
│   │   ├── models/            # 数据模型（Prisma）
│   │   ├── middleware/        # Express 中间件
│   │   ├── utils/             # 工具函数
│   │   ├── config/            # 配置文件
│   │   └── index.ts           # 应用入口
│   ├── prisma/
│   │   └── schema.prisma      # 数据库模型定义
│   ├── tests/                 # 测试文件
│   ├── package.json           # Node.js 依赖配置
│   └── tsconfig.json          # TypeScript 配置
│
├── PRD/                       # 产品需求文档
└── 开发计划与分析.md          # 开发计划文档
```

## 🏗️ 架构设计

### 前端架构（Flutter）

#### 1. 核心层（Core）
- **constants**: 应用常量（颜色、API 端点等）
- **network**: 网络请求封装（Dio 客户端）
- **storage**: 本地存储（Secure Storage、SharedPreferences）
- **utils**: 工具类（路由配置、通用函数）

#### 2. 功能层（Features）
采用功能模块化设计，每个功能模块包含：
- `presentation/`: UI 层（Pages、Widgets）
- `domain/`: 业务逻辑层（Use Cases、Entities）
- `data/`: 数据层（Repositories、Data Sources）

#### 3. 共享层（Shared）
- `widgets/`: 可复用的 UI 组件
- `models/`: 跨模块共享的数据模型

#### 4. 服务层（Services）
- API 服务封装
- 第三方服务集成（Google、Mapbox、Stripe 等）

### 后端架构（Node.js/Express）

#### 1. 控制器层（Controllers）
处理 HTTP 请求，调用服务层

#### 2. 服务层（Services）
业务逻辑实现

#### 3. 模型层（Models）
数据库模型定义（使用 Prisma）

#### 4. 中间件层（Middleware）
- 错误处理
- 认证授权
- 请求日志
- 限流等

## 🔧 技术栈

### 前端
- **框架**: Flutter 3.24+
- **状态管理**: Riverpod
- **路由**: GoRouter
- **网络**: Dio
- **存储**: Hive, SharedPreferences, Flutter Secure Storage
- **地图**: Mapbox Maps Flutter SDK
- **认证**: Google Sign-In, Firebase Auth
- **支付**: Flutter Stripe

### 后端
- **运行时**: Node.js 18+
- **框架**: Express.js
- **语言**: TypeScript
- **ORM**: Prisma
- **数据库**: PostgreSQL 15+
- **缓存**: Redis
- **认证**: JWT
- **支付**: Stripe
- **地图**: Google Maps API

## 📝 下一步开发步骤

1. **安装 Flutter SDK**（如果尚未安装）
   ```bash
   # 参考: https://flutter.dev/docs/get-started/install
   ```

2. **初始化 Flutter 项目**
   ```bash
   cd wanderlog_app
   flutter pub get
   ```

3. **配置环境变量**
   - 复制 `.env.dev.example` 为 `.env.dev`
   - 填入 API Keys 和配置信息

4. **初始化后端项目**
   ```bash
   cd wanderlog_api
   npm install
   ```

5. **配置数据库**
   - 设置 PostgreSQL 数据库
   - 运行 Prisma 迁移：`npm run db:migrate`

6. **开始开发 Phase 2: 用户认证系统**

## 🚀 运行项目

### Flutter App
```bash
cd wanderlog_app
flutter pub get
flutter run -d chrome  # Web
flutter run -d ios    # iOS
flutter run -d android # Android
```

### Backend API
```bash
cd wanderlog_api
npm install
npm run dev
```

## 📚 相关文档

- [开发计划与分析.md](./开发计划与分析.md) - 详细开发计划
- [wanderlog_app/README.md](./wanderlog_app/README.md) - Flutter 应用说明
- [wanderlog_api/README.md](./wanderlog_api/README.md) - 后端 API 说明







