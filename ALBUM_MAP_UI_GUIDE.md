# 相册地点地图页面 - 界面预览

## 页面布局

```
┌─────────────────────────────────────────┐
│ ← 3 day in copenhagen                   │ <- 顶部导航栏（半透明白色渐变）
│   50 spots in Copenhagen                │
├─────────────────────────────────────────┤
│                                         │
│         🗺️ 地图区域                     │
│                                         │
│    📍 Nyhavn    📍 Tivoli              │ <- 地点标记（可点击）
│                                         │
│              📍 Round Tower             │
│                                         │
│  📍 Little                              │
│     Mermaid                             │
│                                         │
│         📍 Rosenborg                    │
│                                         │
├─────────────────────────────────────────┤
│  ┌───────────┐ ┌───────────┐           │
│  │           │ │           │           │ <- 横向滑动卡片列表
│  │  [图片]   │ │  [图片]   │           │
│  │           │ │           │           │
│  ├───────────┤ ├───────────┤           │
│  │#tag #tag  │ │#tag #tag  │           │
│  │Nyhavn     │ │Tivoli     │           │
│  │Harbour    │ │Gardens    │           │
│  │⭐ 4.6 (3k)│ │⭐ 4.7 (5k)│           │
│  └───────────┘ └───────────┘           │
└─────────────────────────────────────────┘
```

## 功能说明

### 1. 顶部导航栏
- **返回按钮**（←）：返回首页
- **相册标题**："3 day in copenhagen"
- **统计信息**："50 spots in Copenhagen"

### 2. 地图区域（铺满屏幕）
- 显示城市所有地点的位置
- 支持双指缩放、平移操作
- 地点标记为带图标的白色/黄色气泡

### 3. 地点标记
```
┌─────────────────┐
│ 🏛️ Nyhavn      │ <- 未选中（白色背景）
└─────────────────┘

┌─────────────────┐
│ 🏛️ Nyhavn      │ <- 选中（黄色背景）
└─────────────────┘
```
- 显示地点图标和名称
- 选中状态为黄色背景，其他为白色
- 点击标记切换到对应卡片

### 4. 底部卡片（横向滑动）
```
┌───────────────┐
│               │ <- 封面图片 (135px 高)
│   [图片]      │
│               │
├───────────────┤
│ #tag1 #tag2   │ <- 标签（最多2个）
│               │
│ Nyhavn Harbour│ <- 地点名称（最多2行）
│               │
│ ⭐ 4.6 (3124) │ <- 评分和评论数
└───────────────┘
```
- 卡片宽度：屏幕宽度的 85%
- 高度：260px
- 图片高度：135px
- 支持横向循环滑动

## 交互流程

### 滑动卡片 → 地图移动
```
用户滑动卡片
    ↓
卡片切换到新地点
    ↓
地图自动移动到该地点中心
    ↓
对应标记变为黄色（选中状态）
```

### 点击地图标记 → 卡片切换
```
用户点击地图标记
    ↓
卡片列表自动滑动到该地点
    ↓
标记变为黄色（选中状态）
```

### 点击卡片 → 查看详情
```
用户点击卡片
    ↓
从底部弹出地点详情弹窗
    ↓
显示完整地点信息（图片、标签、介绍等）
```

## 视觉效果

### 顶部导航栏
- 背景：白色渐变（95% → 0% 透明）
- 避免遮挡地图内容
- 保证文字清晰可读

### 地点标记
- **未选中**：白色背景 + 黑色边框
- **选中**：黄色背景 + 黑色边框
- 阴影：增强立体感

### 卡片样式
- 白色背景
- 黑色边框（2px）
- 圆角设计
- 卡片间距：通过 viewportFraction 实现
- 选中的卡片略大（scale: 1.0 vs 0.92）

## 支持的城市

| 城市        | 地点数 |
|-------------|--------|
| Copenhagen  | 5      |
| Porto       | 1      |
| Paris       | 1      |
| Tokyo       | 1      |
| Barcelona   | 1      |
| Amsterdam   | 1      |

## 示例地点（哥本哈根）

1. **Nyhavn Harbour** - 新港
   - 标签：Architecture, Harbor, History
   - 评分：4.6 ⭐ (3124 评论)

2. **Tivoli Gardens** - 趣伏里花园
   - 标签：Park, Entertainment, Garden
   - 评分：4.7 ⭐ (5892 评论)

3. **The Little Mermaid** - 小美人鱼雕像
   - 标签：Landmark, History, Culture
   - 评分：4.3 ⭐ (4562 评论)

4. **Rosenborg Castle** - 罗森堡城堡
   - 标签：Museum, History, Architecture
   - 评分：4.6 ⭐ (2873 评论)

5. **The Round Tower** - 圆塔
   - 标签：Architecture, History
   - 评分：4.6 ⭐ (1395 评论)

## 使用场景

1. **用户在首页看到相册**
   - 显示："3 day in copenhagen"
   - 点击卡片

2. **进入相册地图页面**
   - 地图自动定位到哥本哈根
   - 显示该城市所有 5 个地点的位置
   - 第一个地点默认选中

3. **浏览地点**
   - 横向滑动卡片查看所有地点
   - 或者点击地图标记切换地点
   - 地图随卡片滑动自动移动

4. **查看详情**
   - 点击卡片查看地点详细信息
   - 包含多张图片、完整介绍、标签等

5. **返回首页**
   - 点击左上角返回按钮
   - 回到首页相册列表
